import type { ProcessedSerializerData } from "../processSerializerData";
export declare function createSerializer<T>(info: ProcessedSerializerData): (value: T) => {
    buffer: buffer;
    blobs: defined[];
};
