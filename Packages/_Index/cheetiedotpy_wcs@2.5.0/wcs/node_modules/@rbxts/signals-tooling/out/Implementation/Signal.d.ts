/// <reference types="@rbxts/compiler-types" />
import { ISignal } from "../Interfaces/ISignal";
import { ISignalConnection } from "../Interfaces/ISignalConnection";
import { AnyArgs } from "../types";
declare class SignalConnection implements ISignalConnection {
    private readonly disconnectCallback;
    Connected: boolean;
    constructor(disconnectCallback: () => void);
    Disconnect(): void;
}
export declare class Signal<T extends AnyArgs = () => void> implements ISignal<T> {
    private connections;
    private connectionsHandlersMap;
    private lastFiredTick;
    private lastFiredArgs?;
    private isDestroyed;
    Connect(onFiredCallback: (...args: Parameters<T>) => void): SignalConnection;
    disconnectAll(): void;
    destroy(): void;
    fire(...args: Parameters<T>): void;
    Wait(): LuaTuple<Parameters<T>>;
}
export {};
